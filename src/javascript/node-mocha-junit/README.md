# Overview

This is a nodejs mocha test project that has a single suite with multiple tests in junit format.

> Note:
>
> Mocha produces an empty root suite in addition to other suites.

## Running

```shell
npm install
npm run test
```
